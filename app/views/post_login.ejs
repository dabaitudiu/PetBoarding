<!DOCTYPE html>
<html>
    <head>
        <title>
            登陆后状态
        </title>
    </head>

    <body>
        <h1>状态：</h1>
        <% if (data.length === 0) {%>
            无此用户
        <% } else { %>
            <% if(data[0].customer_password === password) {%>
                恭喜你！成功登录！
                <form action="search" method="GET">
                    <input type="hidden" name="customer_id" value="<%= data[0].customer_id %>"/>
                    <input type="hidden" name="customer_name" value="<%= data[0].customer_name %>"/>
                    <button type="submit">点击返回主页</button>
                </form>
            <% } else{ %>
                密码错误，<a href="login">点击返回</a> 
            <% } %>
        <% } %>
        

    </body>
</html>